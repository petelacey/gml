from_scratch='false'

while getopts 's' flag; do
  case "${flag}" in
    s) from_scratch='true' ;;
    *) error "Unexpected option ${flag}" ;;
  esac
done

if [ "$from_scratch" = "true" ]
then
  docker-compose run --rm web sh -c "mix ecto.reset"
fi

docker-compose up 
